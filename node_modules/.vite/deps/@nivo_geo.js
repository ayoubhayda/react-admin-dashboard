import {
  Bt,
  Et,
  Fi,
  H,
  Hi,
  Jt,
  Mi,
  We,
  X,
  Ye,
  _i,
  dt,
  format,
  j,
  k,
  require_get,
  require_isFunction,
  tt,
  w
} from "./chunk-S3HLA6MB.js";
import {
  require_prop_types
} from "./chunk-XHDHBZ3M.js";
import "./chunk-XBHG3ID7.js";
import {
  require_jsx_runtime
} from "./chunk-XUXHEDPB.js";
import "./chunk-DYGSSR37.js";
import {
  require_react
} from "./chunk-YSDLPTTY.js";
import {
  __toESM
} from "./chunk-ZS7NZCD4.js";

// node_modules/@nivo/geo/dist/nivo-geo.es.js
var import_react = __toESM(require_react());
var import_prop_types = __toESM(require_prop_types());
var import_isFunction = __toESM(require_isFunction());
var import_get = __toESM(require_get());

// node_modules/d3-geo/src/adder.js
function adder_default() {
  return new Adder();
}
function Adder() {
  this.reset();
}
Adder.prototype = {
  constructor: Adder,
  reset: function() {
    this.s = // rounded value
    this.t = 0;
  },
  add: function(y2) {
    add(temp, y2, this.t);
    add(this, temp.s, this.s);
    if (this.s)
      this.t += temp.t;
    else
      this.s = temp.t;
  },
  valueOf: function() {
    return this.s;
  }
};
var temp = new Adder();
function add(adder, a, b) {
  var x = adder.s = a + b, bv = x - a, av = x - bv;
  adder.t = a - av + (b - bv);
}

// node_modules/d3-geo/src/math.js
var epsilon = 1e-6;
var epsilon2 = 1e-12;
var pi = Math.PI;
var halfPi = pi / 2;
var quarterPi = pi / 4;
var tau = pi * 2;
var degrees = 180 / pi;
var radians = pi / 180;
var abs = Math.abs;
var atan = Math.atan;
var atan2 = Math.atan2;
var cos = Math.cos;
var ceil = Math.ceil;
var exp = Math.exp;
var log = Math.log;
var sin = Math.sin;
var sign = Math.sign || function(x) {
  return x > 0 ? 1 : x < 0 ? -1 : 0;
};
var sqrt = Math.sqrt;
var tan = Math.tan;
function acos(x) {
  return x > 1 ? 0 : x < -1 ? pi : Math.acos(x);
}
function asin(x) {
  return x > 1 ? halfPi : x < -1 ? -halfPi : Math.asin(x);
}

// node_modules/d3-geo/src/noop.js
function noop() {
}

// node_modules/d3-geo/src/stream.js
function streamGeometry(geometry, stream) {
  if (geometry && streamGeometryType.hasOwnProperty(geometry.type)) {
    streamGeometryType[geometry.type](geometry, stream);
  }
}
var streamObjectType = {
  Feature: function(object2, stream) {
    streamGeometry(object2.geometry, stream);
  },
  FeatureCollection: function(object2, stream) {
    var features = object2.features, i2 = -1, n2 = features.length;
    while (++i2 < n2)
      streamGeometry(features[i2].geometry, stream);
  }
};
var streamGeometryType = {
  Sphere: function(object2, stream) {
    stream.sphere();
  },
  Point: function(object2, stream) {
    object2 = object2.coordinates;
    stream.point(object2[0], object2[1], object2[2]);
  },
  MultiPoint: function(object2, stream) {
    var coordinates2 = object2.coordinates, i2 = -1, n2 = coordinates2.length;
    while (++i2 < n2)
      object2 = coordinates2[i2], stream.point(object2[0], object2[1], object2[2]);
  },
  LineString: function(object2, stream) {
    streamLine(object2.coordinates, stream, 0);
  },
  MultiLineString: function(object2, stream) {
    var coordinates2 = object2.coordinates, i2 = -1, n2 = coordinates2.length;
    while (++i2 < n2)
      streamLine(coordinates2[i2], stream, 0);
  },
  Polygon: function(object2, stream) {
    streamPolygon(object2.coordinates, stream);
  },
  MultiPolygon: function(object2, stream) {
    var coordinates2 = object2.coordinates, i2 = -1, n2 = coordinates2.length;
    while (++i2 < n2)
      streamPolygon(coordinates2[i2], stream);
  },
  GeometryCollection: function(object2, stream) {
    var geometries = object2.geometries, i2 = -1, n2 = geometries.length;
    while (++i2 < n2)
      streamGeometry(geometries[i2], stream);
  }
};
function streamLine(coordinates2, stream, closed) {
  var i2 = -1, n2 = coordinates2.length - closed, coordinate;
  stream.lineStart();
  while (++i2 < n2)
    coordinate = coordinates2[i2], stream.point(coordinate[0], coordinate[1], coordinate[2]);
  stream.lineEnd();
}
function streamPolygon(coordinates2, stream) {
  var i2 = -1, n2 = coordinates2.length;
  stream.polygonStart();
  while (++i2 < n2)
    streamLine(coordinates2[i2], stream, 1);
  stream.polygonEnd();
}
function stream_default(object2, stream) {
  if (object2 && streamObjectType.hasOwnProperty(object2.type)) {
    streamObjectType[object2.type](object2, stream);
  } else {
    streamGeometry(object2, stream);
  }
}

// node_modules/d3-geo/src/area.js
var areaRingSum = adder_default();
var areaSum = adder_default();

// node_modules/d3-geo/src/cartesian.js
function spherical(cartesian2) {
  return [atan2(cartesian2[1], cartesian2[0]), asin(cartesian2[2])];
}
function cartesian(spherical2) {
  var lambda = spherical2[0], phi = spherical2[1], cosPhi = cos(phi);
  return [cosPhi * cos(lambda), cosPhi * sin(lambda), sin(phi)];
}
function cartesianDot(a, b) {
  return a[0] * b[0] + a[1] * b[1] + a[2] * b[2];
}
function cartesianCross(a, b) {
  return [a[1] * b[2] - a[2] * b[1], a[2] * b[0] - a[0] * b[2], a[0] * b[1] - a[1] * b[0]];
}
function cartesianAddInPlace(a, b) {
  a[0] += b[0], a[1] += b[1], a[2] += b[2];
}
function cartesianScale(vector, k2) {
  return [vector[0] * k2, vector[1] * k2, vector[2] * k2];
}
function cartesianNormalizeInPlace(d) {
  var l = sqrt(d[0] * d[0] + d[1] * d[1] + d[2] * d[2]);
  d[0] /= l, d[1] /= l, d[2] /= l;
}

// node_modules/d3-geo/src/bounds.js
var deltaSum = adder_default();

// node_modules/d3-geo/src/compose.js
function compose_default(a, b) {
  function compose(x, y2) {
    return x = a(x, y2), b(x[0], x[1]);
  }
  if (a.invert && b.invert)
    compose.invert = function(x, y2) {
      return x = b.invert(x, y2), x && a.invert(x[0], x[1]);
    };
  return compose;
}

// node_modules/d3-geo/src/rotation.js
function rotationIdentity(lambda, phi) {
  return [abs(lambda) > pi ? lambda + Math.round(-lambda / tau) * tau : lambda, phi];
}
rotationIdentity.invert = rotationIdentity;
function rotateRadians(deltaLambda, deltaPhi, deltaGamma) {
  return (deltaLambda %= tau) ? deltaPhi || deltaGamma ? compose_default(rotationLambda(deltaLambda), rotationPhiGamma(deltaPhi, deltaGamma)) : rotationLambda(deltaLambda) : deltaPhi || deltaGamma ? rotationPhiGamma(deltaPhi, deltaGamma) : rotationIdentity;
}
function forwardRotationLambda(deltaLambda) {
  return function(lambda, phi) {
    return lambda += deltaLambda, [lambda > pi ? lambda - tau : lambda < -pi ? lambda + tau : lambda, phi];
  };
}
function rotationLambda(deltaLambda) {
  var rotation = forwardRotationLambda(deltaLambda);
  rotation.invert = forwardRotationLambda(-deltaLambda);
  return rotation;
}
function rotationPhiGamma(deltaPhi, deltaGamma) {
  var cosDeltaPhi = cos(deltaPhi), sinDeltaPhi = sin(deltaPhi), cosDeltaGamma = cos(deltaGamma), sinDeltaGamma = sin(deltaGamma);
  function rotation(lambda, phi) {
    var cosPhi = cos(phi), x = cos(lambda) * cosPhi, y2 = sin(lambda) * cosPhi, z2 = sin(phi), k2 = z2 * cosDeltaPhi + x * sinDeltaPhi;
    return [
      atan2(y2 * cosDeltaGamma - k2 * sinDeltaGamma, x * cosDeltaPhi - z2 * sinDeltaPhi),
      asin(k2 * cosDeltaGamma + y2 * sinDeltaGamma)
    ];
  }
  rotation.invert = function(lambda, phi) {
    var cosPhi = cos(phi), x = cos(lambda) * cosPhi, y2 = sin(lambda) * cosPhi, z2 = sin(phi), k2 = z2 * cosDeltaGamma - y2 * sinDeltaGamma;
    return [
      atan2(y2 * cosDeltaGamma + z2 * sinDeltaGamma, x * cosDeltaPhi + k2 * sinDeltaPhi),
      asin(k2 * cosDeltaPhi - x * sinDeltaPhi)
    ];
  };
  return rotation;
}
function rotation_default(rotate) {
  rotate = rotateRadians(rotate[0] * radians, rotate[1] * radians, rotate.length > 2 ? rotate[2] * radians : 0);
  function forward(coordinates2) {
    coordinates2 = rotate(coordinates2[0] * radians, coordinates2[1] * radians);
    return coordinates2[0] *= degrees, coordinates2[1] *= degrees, coordinates2;
  }
  forward.invert = function(coordinates2) {
    coordinates2 = rotate.invert(coordinates2[0] * radians, coordinates2[1] * radians);
    return coordinates2[0] *= degrees, coordinates2[1] *= degrees, coordinates2;
  };
  return forward;
}

// node_modules/d3-geo/src/circle.js
function circleStream(stream, radius, delta, direction, t0, t1) {
  if (!delta)
    return;
  var cosRadius = cos(radius), sinRadius = sin(radius), step = direction * delta;
  if (t0 == null) {
    t0 = radius + direction * tau;
    t1 = radius - step / 2;
  } else {
    t0 = circleRadius(cosRadius, t0);
    t1 = circleRadius(cosRadius, t1);
    if (direction > 0 ? t0 < t1 : t0 > t1)
      t0 += direction * tau;
  }
  for (var point, t2 = t0; direction > 0 ? t2 > t1 : t2 < t1; t2 -= step) {
    point = spherical([cosRadius, -sinRadius * cos(t2), -sinRadius * sin(t2)]);
    stream.point(point[0], point[1]);
  }
}
function circleRadius(cosRadius, point) {
  point = cartesian(point), point[0] -= cosRadius;
  cartesianNormalizeInPlace(point);
  var radius = acos(-point[1]);
  return ((-point[2] < 0 ? -radius : radius) + tau - epsilon) % tau;
}

// node_modules/d3-geo/src/clip/buffer.js
function buffer_default() {
  var lines = [], line;
  return {
    point: function(x, y2, m2) {
      line.push([x, y2, m2]);
    },
    lineStart: function() {
      lines.push(line = []);
    },
    lineEnd: noop,
    rejoin: function() {
      if (lines.length > 1)
        lines.push(lines.pop().concat(lines.shift()));
    },
    result: function() {
      var result = lines;
      lines = [];
      line = null;
      return result;
    }
  };
}

// node_modules/d3-geo/src/pointEqual.js
function pointEqual_default(a, b) {
  return abs(a[0] - b[0]) < epsilon && abs(a[1] - b[1]) < epsilon;
}

// node_modules/d3-geo/src/clip/rejoin.js
function Intersection(point, points, other, entry) {
  this.x = point;
  this.z = points;
  this.o = other;
  this.e = entry;
  this.v = false;
  this.n = this.p = null;
}
function rejoin_default(segments, compareIntersection2, startInside, interpolate, stream) {
  var subject = [], clip = [], i2, n2;
  segments.forEach(function(segment) {
    if ((n3 = segment.length - 1) <= 0)
      return;
    var n3, p0 = segment[0], p1 = segment[n3], x;
    if (pointEqual_default(p0, p1)) {
      if (!p0[2] && !p1[2]) {
        stream.lineStart();
        for (i2 = 0; i2 < n3; ++i2)
          stream.point((p0 = segment[i2])[0], p0[1]);
        stream.lineEnd();
        return;
      }
      p1[0] += 2 * epsilon;
    }
    subject.push(x = new Intersection(p0, segment, null, true));
    clip.push(x.o = new Intersection(p0, null, x, false));
    subject.push(x = new Intersection(p1, segment, null, false));
    clip.push(x.o = new Intersection(p1, null, x, true));
  });
  if (!subject.length)
    return;
  clip.sort(compareIntersection2);
  link(subject);
  link(clip);
  for (i2 = 0, n2 = clip.length; i2 < n2; ++i2) {
    clip[i2].e = startInside = !startInside;
  }
  var start = subject[0], points, point;
  while (1) {
    var current = start, isSubject = true;
    while (current.v)
      if ((current = current.n) === start)
        return;
    points = current.z;
    stream.lineStart();
    do {
      current.v = current.o.v = true;
      if (current.e) {
        if (isSubject) {
          for (i2 = 0, n2 = points.length; i2 < n2; ++i2)
            stream.point((point = points[i2])[0], point[1]);
        } else {
          interpolate(current.x, current.n.x, 1, stream);
        }
        current = current.n;
      } else {
        if (isSubject) {
          points = current.p.z;
          for (i2 = points.length - 1; i2 >= 0; --i2)
            stream.point((point = points[i2])[0], point[1]);
        } else {
          interpolate(current.x, current.p.x, -1, stream);
        }
        current = current.p;
      }
      current = current.o;
      points = current.z;
      isSubject = !isSubject;
    } while (!current.v);
    stream.lineEnd();
  }
}
function link(array2) {
  if (!(n2 = array2.length))
    return;
  var n2, i2 = 0, a = array2[0], b;
  while (++i2 < n2) {
    a.n = b = array2[i2];
    b.p = a;
    a = b;
  }
  a.n = b = array2[0];
  b.p = a;
}

// node_modules/d3-geo/src/polygonContains.js
var sum = adder_default();
function longitude(point) {
  if (abs(point[0]) <= pi)
    return point[0];
  else
    return sign(point[0]) * ((abs(point[0]) + pi) % tau - pi);
}
function polygonContains_default(polygon, point) {
  var lambda = longitude(point), phi = point[1], sinPhi = sin(phi), normal = [sin(lambda), -cos(lambda), 0], angle = 0, winding = 0;
  sum.reset();
  if (sinPhi === 1)
    phi = halfPi + epsilon;
  else if (sinPhi === -1)
    phi = -halfPi - epsilon;
  for (var i2 = 0, n2 = polygon.length; i2 < n2; ++i2) {
    if (!(m2 = (ring = polygon[i2]).length))
      continue;
    var ring, m2, point0 = ring[m2 - 1], lambda02 = longitude(point0), phi0 = point0[1] / 2 + quarterPi, sinPhi02 = sin(phi0), cosPhi02 = cos(phi0);
    for (var j2 = 0; j2 < m2; ++j2, lambda02 = lambda1, sinPhi02 = sinPhi1, cosPhi02 = cosPhi1, point0 = point1) {
      var point1 = ring[j2], lambda1 = longitude(point1), phi1 = point1[1] / 2 + quarterPi, sinPhi1 = sin(phi1), cosPhi1 = cos(phi1), delta = lambda1 - lambda02, sign2 = delta >= 0 ? 1 : -1, absDelta = sign2 * delta, antimeridian = absDelta > pi, k2 = sinPhi02 * sinPhi1;
      sum.add(atan2(k2 * sign2 * sin(absDelta), cosPhi02 * cosPhi1 + k2 * cos(absDelta)));
      angle += antimeridian ? delta + sign2 * tau : delta;
      if (antimeridian ^ lambda02 >= lambda ^ lambda1 >= lambda) {
        var arc = cartesianCross(cartesian(point0), cartesian(point1));
        cartesianNormalizeInPlace(arc);
        var intersection = cartesianCross(normal, arc);
        cartesianNormalizeInPlace(intersection);
        var phiArc = (antimeridian ^ delta >= 0 ? -1 : 1) * asin(intersection[2]);
        if (phi > phiArc || phi === phiArc && (arc[0] || arc[1])) {
          winding += antimeridian ^ delta >= 0 ? 1 : -1;
        }
      }
    }
  }
  return (angle < -epsilon || angle < epsilon && sum < -epsilon) ^ winding & 1;
}

// node_modules/d3-array/src/ascending.js
function ascending_default(a, b) {
  return a < b ? -1 : a > b ? 1 : a >= b ? 0 : NaN;
}

// node_modules/d3-array/src/bisector.js
function bisector_default(compare) {
  if (compare.length === 1)
    compare = ascendingComparator(compare);
  return {
    left: function(a, x, lo, hi) {
      if (lo == null)
        lo = 0;
      if (hi == null)
        hi = a.length;
      while (lo < hi) {
        var mid = lo + hi >>> 1;
        if (compare(a[mid], x) < 0)
          lo = mid + 1;
        else
          hi = mid;
      }
      return lo;
    },
    right: function(a, x, lo, hi) {
      if (lo == null)
        lo = 0;
      if (hi == null)
        hi = a.length;
      while (lo < hi) {
        var mid = lo + hi >>> 1;
        if (compare(a[mid], x) > 0)
          hi = mid;
        else
          lo = mid + 1;
      }
      return lo;
    }
  };
}
function ascendingComparator(f) {
  return function(d, x) {
    return ascending_default(f(d), x);
  };
}

// node_modules/d3-array/src/bisect.js
var ascendingBisect = bisector_default(ascending_default);
var bisectRight = ascendingBisect.right;
var bisectLeft = ascendingBisect.left;

// node_modules/d3-array/src/array.js
var array = Array.prototype;
var slice = array.slice;
var map = array.map;

// node_modules/d3-array/src/range.js
function range_default(start, stop, step) {
  start = +start, stop = +stop, step = (n2 = arguments.length) < 2 ? (stop = start, start = 0, 1) : n2 < 3 ? 1 : +step;
  var i2 = -1, n2 = Math.max(0, Math.ceil((stop - start) / step)) | 0, range = new Array(n2);
  while (++i2 < n2) {
    range[i2] = start + i2 * step;
  }
  return range;
}

// node_modules/d3-array/src/ticks.js
var e10 = Math.sqrt(50);
var e5 = Math.sqrt(10);
var e2 = Math.sqrt(2);

// node_modules/d3-array/src/merge.js
function merge_default(arrays) {
  var n2 = arrays.length, m2, i2 = -1, j2 = 0, merged, array2;
  while (++i2 < n2)
    j2 += arrays[i2].length;
  merged = new Array(j2);
  while (--n2 >= 0) {
    array2 = arrays[n2];
    m2 = array2.length;
    while (--m2 >= 0) {
      merged[--j2] = array2[m2];
    }
  }
  return merged;
}

// node_modules/d3-geo/src/clip/index.js
function clip_default(pointVisible, clipLine, interpolate, start) {
  return function(sink) {
    var line = clipLine(sink), ringBuffer = buffer_default(), ringSink = clipLine(ringBuffer), polygonStarted = false, polygon, segments, ring;
    var clip = {
      point,
      lineStart,
      lineEnd,
      polygonStart: function() {
        clip.point = pointRing;
        clip.lineStart = ringStart;
        clip.lineEnd = ringEnd;
        segments = [];
        polygon = [];
      },
      polygonEnd: function() {
        clip.point = point;
        clip.lineStart = lineStart;
        clip.lineEnd = lineEnd;
        segments = merge_default(segments);
        var startInside = polygonContains_default(polygon, start);
        if (segments.length) {
          if (!polygonStarted)
            sink.polygonStart(), polygonStarted = true;
          rejoin_default(segments, compareIntersection, startInside, interpolate, sink);
        } else if (startInside) {
          if (!polygonStarted)
            sink.polygonStart(), polygonStarted = true;
          sink.lineStart();
          interpolate(null, null, 1, sink);
          sink.lineEnd();
        }
        if (polygonStarted)
          sink.polygonEnd(), polygonStarted = false;
        segments = polygon = null;
      },
      sphere: function() {
        sink.polygonStart();
        sink.lineStart();
        interpolate(null, null, 1, sink);
        sink.lineEnd();
        sink.polygonEnd();
      }
    };
    function point(lambda, phi) {
      if (pointVisible(lambda, phi))
        sink.point(lambda, phi);
    }
    function pointLine(lambda, phi) {
      line.point(lambda, phi);
    }
    function lineStart() {
      clip.point = pointLine;
      line.lineStart();
    }
    function lineEnd() {
      clip.point = point;
      line.lineEnd();
    }
    function pointRing(lambda, phi) {
      ring.push([lambda, phi]);
      ringSink.point(lambda, phi);
    }
    function ringStart() {
      ringSink.lineStart();
      ring = [];
    }
    function ringEnd() {
      pointRing(ring[0][0], ring[0][1]);
      ringSink.lineEnd();
      var clean = ringSink.clean(), ringSegments = ringBuffer.result(), i2, n2 = ringSegments.length, m2, segment, point2;
      ring.pop();
      polygon.push(ring);
      ring = null;
      if (!n2)
        return;
      if (clean & 1) {
        segment = ringSegments[0];
        if ((m2 = segment.length - 1) > 0) {
          if (!polygonStarted)
            sink.polygonStart(), polygonStarted = true;
          sink.lineStart();
          for (i2 = 0; i2 < m2; ++i2)
            sink.point((point2 = segment[i2])[0], point2[1]);
          sink.lineEnd();
        }
        return;
      }
      if (n2 > 1 && clean & 2)
        ringSegments.push(ringSegments.pop().concat(ringSegments.shift()));
      segments.push(ringSegments.filter(validSegment));
    }
    return clip;
  };
}
function validSegment(segment) {
  return segment.length > 1;
}
function compareIntersection(a, b) {
  return ((a = a.x)[0] < 0 ? a[1] - halfPi - epsilon : halfPi - a[1]) - ((b = b.x)[0] < 0 ? b[1] - halfPi - epsilon : halfPi - b[1]);
}

// node_modules/d3-geo/src/clip/antimeridian.js
var antimeridian_default = clip_default(
  function() {
    return true;
  },
  clipAntimeridianLine,
  clipAntimeridianInterpolate,
  [-pi, -halfPi]
);
function clipAntimeridianLine(stream) {
  var lambda02 = NaN, phi0 = NaN, sign0 = NaN, clean;
  return {
    lineStart: function() {
      stream.lineStart();
      clean = 1;
    },
    point: function(lambda1, phi1) {
      var sign1 = lambda1 > 0 ? pi : -pi, delta = abs(lambda1 - lambda02);
      if (abs(delta - pi) < epsilon) {
        stream.point(lambda02, phi0 = (phi0 + phi1) / 2 > 0 ? halfPi : -halfPi);
        stream.point(sign0, phi0);
        stream.lineEnd();
        stream.lineStart();
        stream.point(sign1, phi0);
        stream.point(lambda1, phi0);
        clean = 0;
      } else if (sign0 !== sign1 && delta >= pi) {
        if (abs(lambda02 - sign0) < epsilon)
          lambda02 -= sign0 * epsilon;
        if (abs(lambda1 - sign1) < epsilon)
          lambda1 -= sign1 * epsilon;
        phi0 = clipAntimeridianIntersect(lambda02, phi0, lambda1, phi1);
        stream.point(sign0, phi0);
        stream.lineEnd();
        stream.lineStart();
        stream.point(sign1, phi0);
        clean = 0;
      }
      stream.point(lambda02 = lambda1, phi0 = phi1);
      sign0 = sign1;
    },
    lineEnd: function() {
      stream.lineEnd();
      lambda02 = phi0 = NaN;
    },
    clean: function() {
      return 2 - clean;
    }
  };
}
function clipAntimeridianIntersect(lambda02, phi0, lambda1, phi1) {
  var cosPhi02, cosPhi1, sinLambda0Lambda1 = sin(lambda02 - lambda1);
  return abs(sinLambda0Lambda1) > epsilon ? atan((sin(phi0) * (cosPhi1 = cos(phi1)) * sin(lambda1) - sin(phi1) * (cosPhi02 = cos(phi0)) * sin(lambda02)) / (cosPhi02 * cosPhi1 * sinLambda0Lambda1)) : (phi0 + phi1) / 2;
}
function clipAntimeridianInterpolate(from, to, direction, stream) {
  var phi;
  if (from == null) {
    phi = direction * halfPi;
    stream.point(-pi, phi);
    stream.point(0, phi);
    stream.point(pi, phi);
    stream.point(pi, 0);
    stream.point(pi, -phi);
    stream.point(0, -phi);
    stream.point(-pi, -phi);
    stream.point(-pi, 0);
    stream.point(-pi, phi);
  } else if (abs(from[0] - to[0]) > epsilon) {
    var lambda = from[0] < to[0] ? pi : -pi;
    phi = direction * lambda / 2;
    stream.point(-lambda, phi);
    stream.point(0, phi);
    stream.point(lambda, phi);
  } else {
    stream.point(to[0], to[1]);
  }
}

// node_modules/d3-geo/src/clip/circle.js
function circle_default(radius) {
  var cr = cos(radius), delta = 6 * radians, smallRadius = cr > 0, notHemisphere = abs(cr) > epsilon;
  function interpolate(from, to, direction, stream) {
    circleStream(stream, radius, delta, direction, from, to);
  }
  function visible(lambda, phi) {
    return cos(lambda) * cos(phi) > cr;
  }
  function clipLine(stream) {
    var point0, c0, v0, v00, clean;
    return {
      lineStart: function() {
        v00 = v0 = false;
        clean = 1;
      },
      point: function(lambda, phi) {
        var point1 = [lambda, phi], point2, v = visible(lambda, phi), c = smallRadius ? v ? 0 : code(lambda, phi) : v ? code(lambda + (lambda < 0 ? pi : -pi), phi) : 0;
        if (!point0 && (v00 = v0 = v))
          stream.lineStart();
        if (v !== v0) {
          point2 = intersect(point0, point1);
          if (!point2 || pointEqual_default(point0, point2) || pointEqual_default(point1, point2))
            point1[2] = 1;
        }
        if (v !== v0) {
          clean = 0;
          if (v) {
            stream.lineStart();
            point2 = intersect(point1, point0);
            stream.point(point2[0], point2[1]);
          } else {
            point2 = intersect(point0, point1);
            stream.point(point2[0], point2[1], 2);
            stream.lineEnd();
          }
          point0 = point2;
        } else if (notHemisphere && point0 && smallRadius ^ v) {
          var t2;
          if (!(c & c0) && (t2 = intersect(point1, point0, true))) {
            clean = 0;
            if (smallRadius) {
              stream.lineStart();
              stream.point(t2[0][0], t2[0][1]);
              stream.point(t2[1][0], t2[1][1]);
              stream.lineEnd();
            } else {
              stream.point(t2[1][0], t2[1][1]);
              stream.lineEnd();
              stream.lineStart();
              stream.point(t2[0][0], t2[0][1], 3);
            }
          }
        }
        if (v && (!point0 || !pointEqual_default(point0, point1))) {
          stream.point(point1[0], point1[1]);
        }
        point0 = point1, v0 = v, c0 = c;
      },
      lineEnd: function() {
        if (v0)
          stream.lineEnd();
        point0 = null;
      },
      // Rejoin first and last segments if there were intersections and the first
      // and last points were visible.
      clean: function() {
        return clean | (v00 && v0) << 1;
      }
    };
  }
  function intersect(a, b, two) {
    var pa = cartesian(a), pb = cartesian(b);
    var n1 = [1, 0, 0], n2 = cartesianCross(pa, pb), n2n2 = cartesianDot(n2, n2), n1n2 = n2[0], determinant = n2n2 - n1n2 * n1n2;
    if (!determinant)
      return !two && a;
    var c1 = cr * n2n2 / determinant, c2 = -cr * n1n2 / determinant, n1xn2 = cartesianCross(n1, n2), A5 = cartesianScale(n1, c1), B = cartesianScale(n2, c2);
    cartesianAddInPlace(A5, B);
    var u = n1xn2, w2 = cartesianDot(A5, u), uu = cartesianDot(u, u), t2 = w2 * w2 - uu * (cartesianDot(A5, A5) - 1);
    if (t2 < 0)
      return;
    var t3 = sqrt(t2), q = cartesianScale(u, (-w2 - t3) / uu);
    cartesianAddInPlace(q, A5);
    q = spherical(q);
    if (!two)
      return q;
    var lambda02 = a[0], lambda1 = b[0], phi0 = a[1], phi1 = b[1], z2;
    if (lambda1 < lambda02)
      z2 = lambda02, lambda02 = lambda1, lambda1 = z2;
    var delta2 = lambda1 - lambda02, polar = abs(delta2 - pi) < epsilon, meridian = polar || delta2 < epsilon;
    if (!polar && phi1 < phi0)
      z2 = phi0, phi0 = phi1, phi1 = z2;
    if (meridian ? polar ? phi0 + phi1 > 0 ^ q[1] < (abs(q[0] - lambda02) < epsilon ? phi0 : phi1) : phi0 <= q[1] && q[1] <= phi1 : delta2 > pi ^ (lambda02 <= q[0] && q[0] <= lambda1)) {
      var q1 = cartesianScale(u, (-w2 + t3) / uu);
      cartesianAddInPlace(q1, A5);
      return [q, spherical(q1)];
    }
  }
  function code(lambda, phi) {
    var r2 = smallRadius ? radius : pi - radius, code2 = 0;
    if (lambda < -r2)
      code2 |= 1;
    else if (lambda > r2)
      code2 |= 2;
    if (phi < -r2)
      code2 |= 4;
    else if (phi > r2)
      code2 |= 8;
    return code2;
  }
  return clip_default(visible, clipLine, interpolate, smallRadius ? [0, -radius] : [-pi, radius - pi]);
}

// node_modules/d3-geo/src/clip/line.js
function line_default(a, b, x05, y05, x12, y12) {
  var ax = a[0], ay = a[1], bx = b[0], by = b[1], t0 = 0, t1 = 1, dx = bx - ax, dy = by - ay, r2;
  r2 = x05 - ax;
  if (!dx && r2 > 0)
    return;
  r2 /= dx;
  if (dx < 0) {
    if (r2 < t0)
      return;
    if (r2 < t1)
      t1 = r2;
  } else if (dx > 0) {
    if (r2 > t1)
      return;
    if (r2 > t0)
      t0 = r2;
  }
  r2 = x12 - ax;
  if (!dx && r2 < 0)
    return;
  r2 /= dx;
  if (dx < 0) {
    if (r2 > t1)
      return;
    if (r2 > t0)
      t0 = r2;
  } else if (dx > 0) {
    if (r2 < t0)
      return;
    if (r2 < t1)
      t1 = r2;
  }
  r2 = y05 - ay;
  if (!dy && r2 > 0)
    return;
  r2 /= dy;
  if (dy < 0) {
    if (r2 < t0)
      return;
    if (r2 < t1)
      t1 = r2;
  } else if (dy > 0) {
    if (r2 > t1)
      return;
    if (r2 > t0)
      t0 = r2;
  }
  r2 = y12 - ay;
  if (!dy && r2 < 0)
    return;
  r2 /= dy;
  if (dy < 0) {
    if (r2 > t1)
      return;
    if (r2 > t0)
      t0 = r2;
  } else if (dy > 0) {
    if (r2 < t0)
      return;
    if (r2 < t1)
      t1 = r2;
  }
  if (t0 > 0)
    a[0] = ax + t0 * dx, a[1] = ay + t0 * dy;
  if (t1 < 1)
    b[0] = ax + t1 * dx, b[1] = ay + t1 * dy;
  return true;
}

// node_modules/d3-geo/src/clip/rectangle.js
var clipMax = 1e9;
var clipMin = -clipMax;
function clipRectangle(x05, y05, x12, y12) {
  function visible(x, y2) {
    return x05 <= x && x <= x12 && y05 <= y2 && y2 <= y12;
  }
  function interpolate(from, to, direction, stream) {
    var a = 0, a1 = 0;
    if (from == null || (a = corner(from, direction)) !== (a1 = corner(to, direction)) || comparePoint(from, to) < 0 ^ direction > 0) {
      do
        stream.point(a === 0 || a === 3 ? x05 : x12, a > 1 ? y12 : y05);
      while ((a = (a + direction + 4) % 4) !== a1);
    } else {
      stream.point(to[0], to[1]);
    }
  }
  function corner(p, direction) {
    return abs(p[0] - x05) < epsilon ? direction > 0 ? 0 : 3 : abs(p[0] - x12) < epsilon ? direction > 0 ? 2 : 1 : abs(p[1] - y05) < epsilon ? direction > 0 ? 1 : 0 : direction > 0 ? 3 : 2;
  }
  function compareIntersection2(a, b) {
    return comparePoint(a.x, b.x);
  }
  function comparePoint(a, b) {
    var ca = corner(a, 1), cb = corner(b, 1);
    return ca !== cb ? ca - cb : ca === 0 ? b[1] - a[1] : ca === 1 ? a[0] - b[0] : ca === 2 ? a[1] - b[1] : b[0] - a[0];
  }
  return function(stream) {
    var activeStream = stream, bufferStream = buffer_default(), segments, polygon, ring, x__, y__, v__, x_, y_, v_, first, clean;
    var clipStream = {
      point,
      lineStart,
      lineEnd,
      polygonStart,
      polygonEnd
    };
    function point(x, y2) {
      if (visible(x, y2))
        activeStream.point(x, y2);
    }
    function polygonInside() {
      var winding = 0;
      for (var i2 = 0, n2 = polygon.length; i2 < n2; ++i2) {
        for (var ring2 = polygon[i2], j2 = 1, m2 = ring2.length, point2 = ring2[0], a0, a1, b0 = point2[0], b1 = point2[1]; j2 < m2; ++j2) {
          a0 = b0, a1 = b1, point2 = ring2[j2], b0 = point2[0], b1 = point2[1];
          if (a1 <= y12) {
            if (b1 > y12 && (b0 - a0) * (y12 - a1) > (b1 - a1) * (x05 - a0))
              ++winding;
          } else {
            if (b1 <= y12 && (b0 - a0) * (y12 - a1) < (b1 - a1) * (x05 - a0))
              --winding;
          }
        }
      }
      return winding;
    }
    function polygonStart() {
      activeStream = bufferStream, segments = [], polygon = [], clean = true;
    }
    function polygonEnd() {
      var startInside = polygonInside(), cleanInside = clean && startInside, visible2 = (segments = merge_default(segments)).length;
      if (cleanInside || visible2) {
        stream.polygonStart();
        if (cleanInside) {
          stream.lineStart();
          interpolate(null, null, 1, stream);
          stream.lineEnd();
        }
        if (visible2) {
          rejoin_default(segments, compareIntersection2, startInside, interpolate, stream);
        }
        stream.polygonEnd();
      }
      activeStream = stream, segments = polygon = ring = null;
    }
    function lineStart() {
      clipStream.point = linePoint;
      if (polygon)
        polygon.push(ring = []);
      first = true;
      v_ = false;
      x_ = y_ = NaN;
    }
    function lineEnd() {
      if (segments) {
        linePoint(x__, y__);
        if (v__ && v_)
          bufferStream.rejoin();
        segments.push(bufferStream.result());
      }
      clipStream.point = point;
      if (v_)
        activeStream.lineEnd();
    }
    function linePoint(x, y2) {
      var v = visible(x, y2);
      if (polygon)
        ring.push([x, y2]);
      if (first) {
        x__ = x, y__ = y2, v__ = v;
        first = false;
        if (v) {
          activeStream.lineStart();
          activeStream.point(x, y2);
        }
      } else {
        if (v && v_)
          activeStream.point(x, y2);
        else {
          var a = [x_ = Math.max(clipMin, Math.min(clipMax, x_)), y_ = Math.max(clipMin, Math.min(clipMax, y_))], b = [x = Math.max(clipMin, Math.min(clipMax, x)), y2 = Math.max(clipMin, Math.min(clipMax, y2))];
          if (line_default(a, b, x05, y05, x12, y12)) {
            if (!v_) {
              activeStream.lineStart();
              activeStream.point(a[0], a[1]);
            }
            activeStream.point(b[0], b[1]);
            if (!v)
              activeStream.lineEnd();
            clean = false;
          } else if (v) {
            activeStream.lineStart();
            activeStream.point(x, y2);
            clean = false;
          }
        }
      }
      x_ = x, y_ = y2, v_ = v;
    }
    return clipStream;
  };
}

// node_modules/d3-geo/src/length.js
var lengthSum = adder_default();
var lambda0;
var sinPhi0;
var cosPhi0;
var lengthStream = {
  sphere: noop,
  point: noop,
  lineStart: lengthLineStart,
  lineEnd: noop,
  polygonStart: noop,
  polygonEnd: noop
};
function lengthLineStart() {
  lengthStream.point = lengthPointFirst;
  lengthStream.lineEnd = lengthLineEnd;
}
function lengthLineEnd() {
  lengthStream.point = lengthStream.lineEnd = noop;
}
function lengthPointFirst(lambda, phi) {
  lambda *= radians, phi *= radians;
  lambda0 = lambda, sinPhi0 = sin(phi), cosPhi0 = cos(phi);
  lengthStream.point = lengthPoint;
}
function lengthPoint(lambda, phi) {
  lambda *= radians, phi *= radians;
  var sinPhi = sin(phi), cosPhi = cos(phi), delta = abs(lambda - lambda0), cosDelta = cos(delta), sinDelta = sin(delta), x = cosPhi * sinDelta, y2 = cosPhi0 * sinPhi - sinPhi0 * cosPhi * cosDelta, z2 = sinPhi0 * sinPhi + cosPhi0 * cosPhi * cosDelta;
  lengthSum.add(atan2(sqrt(x * x + y2 * y2), z2));
  lambda0 = lambda, sinPhi0 = sinPhi, cosPhi0 = cosPhi;
}
function length_default(object2) {
  lengthSum.reset();
  stream_default(object2, lengthStream);
  return +lengthSum;
}

// node_modules/d3-geo/src/distance.js
var coordinates = [null, null];
var object = { type: "LineString", coordinates };
function distance_default(a, b) {
  coordinates[0] = a;
  coordinates[1] = b;
  return length_default(object);
}

// node_modules/d3-geo/src/contains.js
var containsObjectType = {
  Feature: function(object2, point) {
    return containsGeometry(object2.geometry, point);
  },
  FeatureCollection: function(object2, point) {
    var features = object2.features, i2 = -1, n2 = features.length;
    while (++i2 < n2)
      if (containsGeometry(features[i2].geometry, point))
        return true;
    return false;
  }
};
var containsGeometryType = {
  Sphere: function() {
    return true;
  },
  Point: function(object2, point) {
    return containsPoint(object2.coordinates, point);
  },
  MultiPoint: function(object2, point) {
    var coordinates2 = object2.coordinates, i2 = -1, n2 = coordinates2.length;
    while (++i2 < n2)
      if (containsPoint(coordinates2[i2], point))
        return true;
    return false;
  },
  LineString: function(object2, point) {
    return containsLine(object2.coordinates, point);
  },
  MultiLineString: function(object2, point) {
    var coordinates2 = object2.coordinates, i2 = -1, n2 = coordinates2.length;
    while (++i2 < n2)
      if (containsLine(coordinates2[i2], point))
        return true;
    return false;
  },
  Polygon: function(object2, point) {
    return containsPolygon(object2.coordinates, point);
  },
  MultiPolygon: function(object2, point) {
    var coordinates2 = object2.coordinates, i2 = -1, n2 = coordinates2.length;
    while (++i2 < n2)
      if (containsPolygon(coordinates2[i2], point))
        return true;
    return false;
  },
  GeometryCollection: function(object2, point) {
    var geometries = object2.geometries, i2 = -1, n2 = geometries.length;
    while (++i2 < n2)
      if (containsGeometry(geometries[i2], point))
        return true;
    return false;
  }
};
function containsGeometry(geometry, point) {
  return geometry && containsGeometryType.hasOwnProperty(geometry.type) ? containsGeometryType[geometry.type](geometry, point) : false;
}
function containsPoint(coordinates2, point) {
  return distance_default(coordinates2, point) === 0;
}
function containsLine(coordinates2, point) {
  var ao, bo, ab;
  for (var i2 = 0, n2 = coordinates2.length; i2 < n2; i2++) {
    bo = distance_default(coordinates2[i2], point);
    if (bo === 0)
      return true;
    if (i2 > 0) {
      ab = distance_default(coordinates2[i2], coordinates2[i2 - 1]);
      if (ab > 0 && ao <= ab && bo <= ab && (ao + bo - ab) * (1 - Math.pow((ao - bo) / ab, 2)) < epsilon2 * ab)
        return true;
    }
    ao = bo;
  }
  return false;
}
function containsPolygon(coordinates2, point) {
  return !!polygonContains_default(coordinates2.map(ringRadians), pointRadians(point));
}
function ringRadians(ring) {
  return ring = ring.map(pointRadians), ring.pop(), ring;
}
function pointRadians(point) {
  return [point[0] * radians, point[1] * radians];
}
function contains_default(object2, point) {
  return (object2 && containsObjectType.hasOwnProperty(object2.type) ? containsObjectType[object2.type] : containsGeometry)(object2, point);
}

// node_modules/d3-geo/src/graticule.js
function graticuleX(y05, y12, dy) {
  var y2 = range_default(y05, y12 - epsilon, dy).concat(y12);
  return function(x) {
    return y2.map(function(y3) {
      return [x, y3];
    });
  };
}
function graticuleY(x05, x12, dx) {
  var x = range_default(x05, x12 - epsilon, dx).concat(x12);
  return function(y2) {
    return x.map(function(x2) {
      return [x2, y2];
    });
  };
}
function graticule() {
  var x12, x05, X12, X02, y12, y05, Y12, Y02, dx = 10, dy = dx, DX = 90, DY = 360, x, y2, X4, Y3, precision = 2.5;
  function graticule2() {
    return { type: "MultiLineString", coordinates: lines() };
  }
  function lines() {
    return range_default(ceil(X02 / DX) * DX, X12, DX).map(X4).concat(range_default(ceil(Y02 / DY) * DY, Y12, DY).map(Y3)).concat(range_default(ceil(x05 / dx) * dx, x12, dx).filter(function(x2) {
      return abs(x2 % DX) > epsilon;
    }).map(x)).concat(range_default(ceil(y05 / dy) * dy, y12, dy).filter(function(y3) {
      return abs(y3 % DY) > epsilon;
    }).map(y2));
  }
  graticule2.lines = function() {
    return lines().map(function(coordinates2) {
      return { type: "LineString", coordinates: coordinates2 };
    });
  };
  graticule2.outline = function() {
    return {
      type: "Polygon",
      coordinates: [
        X4(X02).concat(
          Y3(Y12).slice(1),
          X4(X12).reverse().slice(1),
          Y3(Y02).reverse().slice(1)
        )
      ]
    };
  };
  graticule2.extent = function(_2) {
    if (!arguments.length)
      return graticule2.extentMinor();
    return graticule2.extentMajor(_2).extentMinor(_2);
  };
  graticule2.extentMajor = function(_2) {
    if (!arguments.length)
      return [[X02, Y02], [X12, Y12]];
    X02 = +_2[0][0], X12 = +_2[1][0];
    Y02 = +_2[0][1], Y12 = +_2[1][1];
    if (X02 > X12)
      _2 = X02, X02 = X12, X12 = _2;
    if (Y02 > Y12)
      _2 = Y02, Y02 = Y12, Y12 = _2;
    return graticule2.precision(precision);
  };
  graticule2.extentMinor = function(_2) {
    if (!arguments.length)
      return [[x05, y05], [x12, y12]];
    x05 = +_2[0][0], x12 = +_2[1][0];
    y05 = +_2[0][1], y12 = +_2[1][1];
    if (x05 > x12)
      _2 = x05, x05 = x12, x12 = _2;
    if (y05 > y12)
      _2 = y05, y05 = y12, y12 = _2;
    return graticule2.precision(precision);
  };
  graticule2.step = function(_2) {
    if (!arguments.length)
      return graticule2.stepMinor();
    return graticule2.stepMajor(_2).stepMinor(_2);
  };
  graticule2.stepMajor = function(_2) {
    if (!arguments.length)
      return [DX, DY];
    DX = +_2[0], DY = +_2[1];
    return graticule2;
  };
  graticule2.stepMinor = function(_2) {
    if (!arguments.length)
      return [dx, dy];
    dx = +_2[0], dy = +_2[1];
    return graticule2;
  };
  graticule2.precision = function(_2) {
    if (!arguments.length)
      return precision;
    precision = +_2;
    x = graticuleX(y05, y12, 90);
    y2 = graticuleY(x05, x12, precision);
    X4 = graticuleX(Y02, Y12, 90);
    Y3 = graticuleY(X02, X12, precision);
    return graticule2;
  };
  return graticule2.extentMajor([[-180, -90 + epsilon], [180, 90 - epsilon]]).extentMinor([[-180, -80 - epsilon], [180, 80 + epsilon]]);
}

// node_modules/d3-geo/src/identity.js
function identity_default2(x) {
  return x;
}

// node_modules/d3-geo/src/path/area.js
var areaSum2 = adder_default();
var areaRingSum2 = adder_default();
var x00;
var y00;
var x0;
var y0;
var areaStream2 = {
  point: noop,
  lineStart: noop,
  lineEnd: noop,
  polygonStart: function() {
    areaStream2.lineStart = areaRingStart;
    areaStream2.lineEnd = areaRingEnd;
  },
  polygonEnd: function() {
    areaStream2.lineStart = areaStream2.lineEnd = areaStream2.point = noop;
    areaSum2.add(abs(areaRingSum2));
    areaRingSum2.reset();
  },
  result: function() {
    var area = areaSum2 / 2;
    areaSum2.reset();
    return area;
  }
};
function areaRingStart() {
  areaStream2.point = areaPointFirst;
}
function areaPointFirst(x, y2) {
  areaStream2.point = areaPoint;
  x00 = x0 = x, y00 = y0 = y2;
}
function areaPoint(x, y2) {
  areaRingSum2.add(y0 * x - x0 * y2);
  x0 = x, y0 = y2;
}
function areaRingEnd() {
  areaPoint(x00, y00);
}
var area_default = areaStream2;

// node_modules/d3-geo/src/path/bounds.js
var x02 = Infinity;
var y02 = x02;
var x1 = -x02;
var y1 = x1;
var boundsStream = {
  point: boundsPoint,
  lineStart: noop,
  lineEnd: noop,
  polygonStart: noop,
  polygonEnd: noop,
  result: function() {
    var bounds = [[x02, y02], [x1, y1]];
    x1 = y1 = -(y02 = x02 = Infinity);
    return bounds;
  }
};
function boundsPoint(x, y2) {
  if (x < x02)
    x02 = x;
  if (x > x1)
    x1 = x;
  if (y2 < y02)
    y02 = y2;
  if (y2 > y1)
    y1 = y2;
}
var bounds_default = boundsStream;

// node_modules/d3-geo/src/path/centroid.js
var X0 = 0;
var Y0 = 0;
var Z0 = 0;
var X1 = 0;
var Y1 = 0;
var Z1 = 0;
var X2 = 0;
var Y2 = 0;
var Z2 = 0;
var x002;
var y002;
var x03;
var y03;
var centroidStream = {
  point: centroidPoint,
  lineStart: centroidLineStart,
  lineEnd: centroidLineEnd,
  polygonStart: function() {
    centroidStream.lineStart = centroidRingStart;
    centroidStream.lineEnd = centroidRingEnd;
  },
  polygonEnd: function() {
    centroidStream.point = centroidPoint;
    centroidStream.lineStart = centroidLineStart;
    centroidStream.lineEnd = centroidLineEnd;
  },
  result: function() {
    var centroid = Z2 ? [X2 / Z2, Y2 / Z2] : Z1 ? [X1 / Z1, Y1 / Z1] : Z0 ? [X0 / Z0, Y0 / Z0] : [NaN, NaN];
    X0 = Y0 = Z0 = X1 = Y1 = Z1 = X2 = Y2 = Z2 = 0;
    return centroid;
  }
};
function centroidPoint(x, y2) {
  X0 += x;
  Y0 += y2;
  ++Z0;
}
function centroidLineStart() {
  centroidStream.point = centroidPointFirstLine;
}
function centroidPointFirstLine(x, y2) {
  centroidStream.point = centroidPointLine;
  centroidPoint(x03 = x, y03 = y2);
}
function centroidPointLine(x, y2) {
  var dx = x - x03, dy = y2 - y03, z2 = sqrt(dx * dx + dy * dy);
  X1 += z2 * (x03 + x) / 2;
  Y1 += z2 * (y03 + y2) / 2;
  Z1 += z2;
  centroidPoint(x03 = x, y03 = y2);
}
function centroidLineEnd() {
  centroidStream.point = centroidPoint;
}
function centroidRingStart() {
  centroidStream.point = centroidPointFirstRing;
}
function centroidRingEnd() {
  centroidPointRing(x002, y002);
}
function centroidPointFirstRing(x, y2) {
  centroidStream.point = centroidPointRing;
  centroidPoint(x002 = x03 = x, y002 = y03 = y2);
}
function centroidPointRing(x, y2) {
  var dx = x - x03, dy = y2 - y03, z2 = sqrt(dx * dx + dy * dy);
  X1 += z2 * (x03 + x) / 2;
  Y1 += z2 * (y03 + y2) / 2;
  Z1 += z2;
  z2 = y03 * x - x03 * y2;
  X2 += z2 * (x03 + x);
  Y2 += z2 * (y03 + y2);
  Z2 += z2 * 3;
  centroidPoint(x03 = x, y03 = y2);
}
var centroid_default = centroidStream;

// node_modules/d3-geo/src/path/context.js
function PathContext(context) {
  this._context = context;
}
PathContext.prototype = {
  _radius: 4.5,
  pointRadius: function(_2) {
    return this._radius = _2, this;
  },
  polygonStart: function() {
    this._line = 0;
  },
  polygonEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._point = 0;
  },
  lineEnd: function() {
    if (this._line === 0)
      this._context.closePath();
    this._point = NaN;
  },
  point: function(x, y2) {
    switch (this._point) {
      case 0: {
        this._context.moveTo(x, y2);
        this._point = 1;
        break;
      }
      case 1: {
        this._context.lineTo(x, y2);
        break;
      }
      default: {
        this._context.moveTo(x + this._radius, y2);
        this._context.arc(x, y2, this._radius, 0, tau);
        break;
      }
    }
  },
  result: noop
};

// node_modules/d3-geo/src/path/measure.js
var lengthSum2 = adder_default();
var lengthRing;
var x003;
var y003;
var x04;
var y04;
var lengthStream2 = {
  point: noop,
  lineStart: function() {
    lengthStream2.point = lengthPointFirst2;
  },
  lineEnd: function() {
    if (lengthRing)
      lengthPoint2(x003, y003);
    lengthStream2.point = noop;
  },
  polygonStart: function() {
    lengthRing = true;
  },
  polygonEnd: function() {
    lengthRing = null;
  },
  result: function() {
    var length = +lengthSum2;
    lengthSum2.reset();
    return length;
  }
};
function lengthPointFirst2(x, y2) {
  lengthStream2.point = lengthPoint2;
  x003 = x04 = x, y003 = y04 = y2;
}
function lengthPoint2(x, y2) {
  x04 -= x, y04 -= y2;
  lengthSum2.add(sqrt(x04 * x04 + y04 * y04));
  x04 = x, y04 = y2;
}
var measure_default = lengthStream2;

// node_modules/d3-geo/src/path/string.js
function PathString() {
  this._string = [];
}
PathString.prototype = {
  _radius: 4.5,
  _circle: circle(4.5),
  pointRadius: function(_2) {
    if ((_2 = +_2) !== this._radius)
      this._radius = _2, this._circle = null;
    return this;
  },
  polygonStart: function() {
    this._line = 0;
  },
  polygonEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._point = 0;
  },
  lineEnd: function() {
    if (this._line === 0)
      this._string.push("Z");
    this._point = NaN;
  },
  point: function(x, y2) {
    switch (this._point) {
      case 0: {
        this._string.push("M", x, ",", y2);
        this._point = 1;
        break;
      }
      case 1: {
        this._string.push("L", x, ",", y2);
        break;
      }
      default: {
        if (this._circle == null)
          this._circle = circle(this._radius);
        this._string.push("M", x, ",", y2, this._circle);
        break;
      }
    }
  },
  result: function() {
    if (this._string.length) {
      var result = this._string.join("");
      this._string = [];
      return result;
    } else {
      return null;
    }
  }
};
function circle(radius) {
  return "m0," + radius + "a" + radius + "," + radius + " 0 1,1 0," + -2 * radius + "a" + radius + "," + radius + " 0 1,1 0," + 2 * radius + "z";
}

// node_modules/d3-geo/src/path/index.js
function path_default(projection2, context) {
  var pointRadius = 4.5, projectionStream, contextStream;
  function path(object2) {
    if (object2) {
      if (typeof pointRadius === "function")
        contextStream.pointRadius(+pointRadius.apply(this, arguments));
      stream_default(object2, projectionStream(contextStream));
    }
    return contextStream.result();
  }
  path.area = function(object2) {
    stream_default(object2, projectionStream(area_default));
    return area_default.result();
  };
  path.measure = function(object2) {
    stream_default(object2, projectionStream(measure_default));
    return measure_default.result();
  };
  path.bounds = function(object2) {
    stream_default(object2, projectionStream(bounds_default));
    return bounds_default.result();
  };
  path.centroid = function(object2) {
    stream_default(object2, projectionStream(centroid_default));
    return centroid_default.result();
  };
  path.projection = function(_2) {
    return arguments.length ? (projectionStream = _2 == null ? (projection2 = null, identity_default2) : (projection2 = _2).stream, path) : projection2;
  };
  path.context = function(_2) {
    if (!arguments.length)
      return context;
    contextStream = _2 == null ? (context = null, new PathString()) : new PathContext(context = _2);
    if (typeof pointRadius !== "function")
      contextStream.pointRadius(pointRadius);
    return path;
  };
  path.pointRadius = function(_2) {
    if (!arguments.length)
      return pointRadius;
    pointRadius = typeof _2 === "function" ? _2 : (contextStream.pointRadius(+_2), +_2);
    return path;
  };
  return path.projection(projection2).context(context);
}

// node_modules/d3-geo/src/transform.js
function transformer(methods) {
  return function(stream) {
    var s = new TransformStream();
    for (var key in methods)
      s[key] = methods[key];
    s.stream = stream;
    return s;
  };
}
function TransformStream() {
}
TransformStream.prototype = {
  constructor: TransformStream,
  point: function(x, y2) {
    this.stream.point(x, y2);
  },
  sphere: function() {
    this.stream.sphere();
  },
  lineStart: function() {
    this.stream.lineStart();
  },
  lineEnd: function() {
    this.stream.lineEnd();
  },
  polygonStart: function() {
    this.stream.polygonStart();
  },
  polygonEnd: function() {
    this.stream.polygonEnd();
  }
};

// node_modules/d3-geo/src/projection/fit.js
function fit(projection2, fitBounds, object2) {
  var clip = projection2.clipExtent && projection2.clipExtent();
  projection2.scale(150).translate([0, 0]);
  if (clip != null)
    projection2.clipExtent(null);
  stream_default(object2, projection2.stream(bounds_default));
  fitBounds(bounds_default.result());
  if (clip != null)
    projection2.clipExtent(clip);
  return projection2;
}
function fitExtent(projection2, extent, object2) {
  return fit(projection2, function(b) {
    var w2 = extent[1][0] - extent[0][0], h = extent[1][1] - extent[0][1], k2 = Math.min(w2 / (b[1][0] - b[0][0]), h / (b[1][1] - b[0][1])), x = +extent[0][0] + (w2 - k2 * (b[1][0] + b[0][0])) / 2, y2 = +extent[0][1] + (h - k2 * (b[1][1] + b[0][1])) / 2;
    projection2.scale(150 * k2).translate([x, y2]);
  }, object2);
}
function fitSize(projection2, size, object2) {
  return fitExtent(projection2, [[0, 0], size], object2);
}
function fitWidth(projection2, width, object2) {
  return fit(projection2, function(b) {
    var w2 = +width, k2 = w2 / (b[1][0] - b[0][0]), x = (w2 - k2 * (b[1][0] + b[0][0])) / 2, y2 = -k2 * b[0][1];
    projection2.scale(150 * k2).translate([x, y2]);
  }, object2);
}
function fitHeight(projection2, height, object2) {
  return fit(projection2, function(b) {
    var h = +height, k2 = h / (b[1][1] - b[0][1]), x = -k2 * b[0][0], y2 = (h - k2 * (b[1][1] + b[0][1])) / 2;
    projection2.scale(150 * k2).translate([x, y2]);
  }, object2);
}

// node_modules/d3-geo/src/projection/resample.js
var maxDepth = 16;
var cosMinDistance = cos(30 * radians);
function resample_default(project, delta2) {
  return +delta2 ? resample(project, delta2) : resampleNone(project);
}
function resampleNone(project) {
  return transformer({
    point: function(x, y2) {
      x = project(x, y2);
      this.stream.point(x[0], x[1]);
    }
  });
}
function resample(project, delta2) {
  function resampleLineTo(x05, y05, lambda02, a0, b0, c0, x12, y12, lambda1, a1, b1, c1, depth, stream) {
    var dx = x12 - x05, dy = y12 - y05, d2 = dx * dx + dy * dy;
    if (d2 > 4 * delta2 && depth--) {
      var a = a0 + a1, b = b0 + b1, c = c0 + c1, m2 = sqrt(a * a + b * b + c * c), phi2 = asin(c /= m2), lambda2 = abs(abs(c) - 1) < epsilon || abs(lambda02 - lambda1) < epsilon ? (lambda02 + lambda1) / 2 : atan2(b, a), p = project(lambda2, phi2), x2 = p[0], y2 = p[1], dx2 = x2 - x05, dy2 = y2 - y05, dz = dy * dx2 - dx * dy2;
      if (dz * dz / d2 > delta2 || abs((dx * dx2 + dy * dy2) / d2 - 0.5) > 0.3 || a0 * a1 + b0 * b1 + c0 * c1 < cosMinDistance) {
        resampleLineTo(x05, y05, lambda02, a0, b0, c0, x2, y2, lambda2, a /= m2, b /= m2, c, depth, stream);
        stream.point(x2, y2);
        resampleLineTo(x2, y2, lambda2, a, b, c, x12, y12, lambda1, a1, b1, c1, depth, stream);
      }
    }
  }
  return function(stream) {
    var lambda00, x004, y004, a00, b00, c00, lambda02, x05, y05, a0, b0, c0;
    var resampleStream = {
      point,
      lineStart,
      lineEnd,
      polygonStart: function() {
        stream.polygonStart();
        resampleStream.lineStart = ringStart;
      },
      polygonEnd: function() {
        stream.polygonEnd();
        resampleStream.lineStart = lineStart;
      }
    };
    function point(x, y2) {
      x = project(x, y2);
      stream.point(x[0], x[1]);
    }
    function lineStart() {
      x05 = NaN;
      resampleStream.point = linePoint;
      stream.lineStart();
    }
    function linePoint(lambda, phi) {
      var c = cartesian([lambda, phi]), p = project(lambda, phi);
      resampleLineTo(x05, y05, lambda02, a0, b0, c0, x05 = p[0], y05 = p[1], lambda02 = lambda, a0 = c[0], b0 = c[1], c0 = c[2], maxDepth, stream);
      stream.point(x05, y05);
    }
    function lineEnd() {
      resampleStream.point = point;
      stream.lineEnd();
    }
    function ringStart() {
      lineStart();
      resampleStream.point = ringPoint;
      resampleStream.lineEnd = ringEnd;
    }
    function ringPoint(lambda, phi) {
      linePoint(lambda00 = lambda, phi), x004 = x05, y004 = y05, a00 = a0, b00 = b0, c00 = c0;
      resampleStream.point = linePoint;
    }
    function ringEnd() {
      resampleLineTo(x05, y05, lambda02, a0, b0, c0, x004, y004, lambda00, a00, b00, c00, maxDepth, stream);
      resampleStream.lineEnd = lineEnd;
      lineEnd();
    }
    return resampleStream;
  };
}

// node_modules/d3-geo/src/projection/index.js
var transformRadians = transformer({
  point: function(x, y2) {
    this.stream.point(x * radians, y2 * radians);
  }
});
function transformRotate(rotate) {
  return transformer({
    point: function(x, y2) {
      var r2 = rotate(x, y2);
      return this.stream.point(r2[0], r2[1]);
    }
  });
}
function scaleTranslate(k2, dx, dy, sx, sy) {
  function transform(x, y2) {
    x *= sx;
    y2 *= sy;
    return [dx + k2 * x, dy - k2 * y2];
  }
  transform.invert = function(x, y2) {
    return [(x - dx) / k2 * sx, (dy - y2) / k2 * sy];
  };
  return transform;
}
function scaleTranslateRotate(k2, dx, dy, sx, sy, alpha) {
  var cosAlpha = cos(alpha), sinAlpha = sin(alpha), a = cosAlpha * k2, b = sinAlpha * k2, ai = cosAlpha / k2, bi = sinAlpha / k2, ci = (sinAlpha * dy - cosAlpha * dx) / k2, fi = (sinAlpha * dx + cosAlpha * dy) / k2;
  function transform(x, y2) {
    x *= sx;
    y2 *= sy;
    return [a * x - b * y2 + dx, dy - b * x - a * y2];
  }
  transform.invert = function(x, y2) {
    return [sx * (ai * x - bi * y2 + ci), sy * (fi - bi * x - ai * y2)];
  };
  return transform;
}
function projection(project) {
  return projectionMutator(function() {
    return project;
  })();
}
function projectionMutator(projectAt) {
  var project, k2 = 150, x = 480, y2 = 250, lambda = 0, phi = 0, deltaLambda = 0, deltaPhi = 0, deltaGamma = 0, rotate, alpha = 0, sx = 1, sy = 1, theta = null, preclip = antimeridian_default, x05 = null, y05, x12, y12, postclip = identity_default2, delta2 = 0.5, projectResample, projectTransform, projectRotateTransform, cache, cacheStream;
  function projection2(point) {
    return projectRotateTransform(point[0] * radians, point[1] * radians);
  }
  function invert(point) {
    point = projectRotateTransform.invert(point[0], point[1]);
    return point && [point[0] * degrees, point[1] * degrees];
  }
  projection2.stream = function(stream) {
    return cache && cacheStream === stream ? cache : cache = transformRadians(transformRotate(rotate)(preclip(projectResample(postclip(cacheStream = stream)))));
  };
  projection2.preclip = function(_2) {
    return arguments.length ? (preclip = _2, theta = void 0, reset()) : preclip;
  };
  projection2.postclip = function(_2) {
    return arguments.length ? (postclip = _2, x05 = y05 = x12 = y12 = null, reset()) : postclip;
  };
  projection2.clipAngle = function(_2) {
    return arguments.length ? (preclip = +_2 ? circle_default(theta = _2 * radians) : (theta = null, antimeridian_default), reset()) : theta * degrees;
  };
  projection2.clipExtent = function(_2) {
    return arguments.length ? (postclip = _2 == null ? (x05 = y05 = x12 = y12 = null, identity_default2) : clipRectangle(x05 = +_2[0][0], y05 = +_2[0][1], x12 = +_2[1][0], y12 = +_2[1][1]), reset()) : x05 == null ? null : [[x05, y05], [x12, y12]];
  };
  projection2.scale = function(_2) {
    return arguments.length ? (k2 = +_2, recenter()) : k2;
  };
  projection2.translate = function(_2) {
    return arguments.length ? (x = +_2[0], y2 = +_2[1], recenter()) : [x, y2];
  };
  projection2.center = function(_2) {
    return arguments.length ? (lambda = _2[0] % 360 * radians, phi = _2[1] % 360 * radians, recenter()) : [lambda * degrees, phi * degrees];
  };
  projection2.rotate = function(_2) {
    return arguments.length ? (deltaLambda = _2[0] % 360 * radians, deltaPhi = _2[1] % 360 * radians, deltaGamma = _2.length > 2 ? _2[2] % 360 * radians : 0, recenter()) : [deltaLambda * degrees, deltaPhi * degrees, deltaGamma * degrees];
  };
  projection2.angle = function(_2) {
    return arguments.length ? (alpha = _2 % 360 * radians, recenter()) : alpha * degrees;
  };
  projection2.reflectX = function(_2) {
    return arguments.length ? (sx = _2 ? -1 : 1, recenter()) : sx < 0;
  };
  projection2.reflectY = function(_2) {
    return arguments.length ? (sy = _2 ? -1 : 1, recenter()) : sy < 0;
  };
  projection2.precision = function(_2) {
    return arguments.length ? (projectResample = resample_default(projectTransform, delta2 = _2 * _2), reset()) : sqrt(delta2);
  };
  projection2.fitExtent = function(extent, object2) {
    return fitExtent(projection2, extent, object2);
  };
  projection2.fitSize = function(size, object2) {
    return fitSize(projection2, size, object2);
  };
  projection2.fitWidth = function(width, object2) {
    return fitWidth(projection2, width, object2);
  };
  projection2.fitHeight = function(height, object2) {
    return fitHeight(projection2, height, object2);
  };
  function recenter() {
    var center = scaleTranslateRotate(k2, 0, 0, sx, sy, alpha).apply(null, project(lambda, phi)), transform = (alpha ? scaleTranslateRotate : scaleTranslate)(k2, x - center[0], y2 - center[1], sx, sy, alpha);
    rotate = rotateRadians(deltaLambda, deltaPhi, deltaGamma);
    projectTransform = compose_default(project, transform);
    projectRotateTransform = compose_default(rotate, projectTransform);
    projectResample = resample_default(projectTransform, delta2);
    return reset();
  }
  function reset() {
    cache = cacheStream = null;
    return projection2;
  }
  return function() {
    project = projectAt.apply(this, arguments);
    projection2.invert = project.invert && invert;
    return recenter();
  };
}

// node_modules/d3-geo/src/projection/azimuthal.js
function azimuthalRaw(scale) {
  return function(x, y2) {
    var cx = cos(x), cy = cos(y2), k2 = scale(cx * cy);
    return [
      k2 * cy * sin(x),
      k2 * sin(y2)
    ];
  };
}
function azimuthalInvert(angle) {
  return function(x, y2) {
    var z2 = sqrt(x * x + y2 * y2), c = angle(z2), sc = sin(c), cc = cos(c);
    return [
      atan2(x * sc, z2 * cc),
      asin(z2 && y2 * sc / z2)
    ];
  };
}

// node_modules/d3-geo/src/projection/azimuthalEqualArea.js
var azimuthalEqualAreaRaw = azimuthalRaw(function(cxcy) {
  return sqrt(2 / (1 + cxcy));
});
azimuthalEqualAreaRaw.invert = azimuthalInvert(function(z2) {
  return 2 * asin(z2 / 2);
});
function azimuthalEqualArea_default() {
  return projection(azimuthalEqualAreaRaw).scale(124.75).clipAngle(180 - 1e-3);
}

// node_modules/d3-geo/src/projection/azimuthalEquidistant.js
var azimuthalEquidistantRaw = azimuthalRaw(function(c) {
  return (c = acos(c)) && c / sin(c);
});
azimuthalEquidistantRaw.invert = azimuthalInvert(function(z2) {
  return z2;
});
function azimuthalEquidistant_default() {
  return projection(azimuthalEquidistantRaw).scale(79.4188).clipAngle(180 - 1e-3);
}

// node_modules/d3-geo/src/projection/mercator.js
function mercatorRaw(lambda, phi) {
  return [lambda, log(tan((halfPi + phi) / 2))];
}
mercatorRaw.invert = function(x, y2) {
  return [x, 2 * atan(exp(y2)) - halfPi];
};
function mercator_default() {
  return mercatorProjection(mercatorRaw).scale(961 / tau);
}
function mercatorProjection(project) {
  var m2 = projection(project), center = m2.center, scale = m2.scale, translate = m2.translate, clipExtent = m2.clipExtent, x05 = null, y05, x12, y12;
  m2.scale = function(_2) {
    return arguments.length ? (scale(_2), reclip()) : scale();
  };
  m2.translate = function(_2) {
    return arguments.length ? (translate(_2), reclip()) : translate();
  };
  m2.center = function(_2) {
    return arguments.length ? (center(_2), reclip()) : center();
  };
  m2.clipExtent = function(_2) {
    return arguments.length ? (_2 == null ? x05 = y05 = x12 = y12 = null : (x05 = +_2[0][0], y05 = +_2[0][1], x12 = +_2[1][0], y12 = +_2[1][1]), reclip()) : x05 == null ? null : [[x05, y05], [x12, y12]];
  };
  function reclip() {
    var k2 = pi * scale(), t2 = m2(rotation_default(m2.rotate()).invert([0, 0]));
    return clipExtent(x05 == null ? [[t2[0] - k2, t2[1] - k2], [t2[0] + k2, t2[1] + k2]] : project === mercatorRaw ? [[Math.max(t2[0] - k2, x05), y05], [Math.min(t2[0] + k2, x12), y12]] : [[x05, Math.max(t2[1] - k2, y05)], [x12, Math.min(t2[1] + k2, y12)]]);
  }
  return reclip();
}

// node_modules/d3-geo/src/projection/equirectangular.js
function equirectangularRaw(lambda, phi) {
  return [lambda, phi];
}
equirectangularRaw.invert = equirectangularRaw;
function equirectangular_default() {
  return projection(equirectangularRaw).scale(152.63);
}

// node_modules/d3-geo/src/projection/equalEarth.js
var A1 = 1.340264;
var A2 = -0.081106;
var A3 = 893e-6;
var A4 = 3796e-6;
var M = sqrt(3) / 2;
var iterations = 12;
function equalEarthRaw(lambda, phi) {
  var l = asin(M * sin(phi)), l2 = l * l, l6 = l2 * l2 * l2;
  return [
    lambda * cos(l) / (M * (A1 + 3 * A2 * l2 + l6 * (7 * A3 + 9 * A4 * l2))),
    l * (A1 + A2 * l2 + l6 * (A3 + A4 * l2))
  ];
}
equalEarthRaw.invert = function(x, y2) {
  var l = y2, l2 = l * l, l6 = l2 * l2 * l2;
  for (var i2 = 0, delta, fy, fpy; i2 < iterations; ++i2) {
    fy = l * (A1 + A2 * l2 + l6 * (A3 + A4 * l2)) - y2;
    fpy = A1 + 3 * A2 * l2 + l6 * (7 * A3 + 9 * A4 * l2);
    l -= delta = fy / fpy, l2 = l * l, l6 = l2 * l2 * l2;
    if (abs(delta) < epsilon2)
      break;
  }
  return [
    M * x * (A1 + 3 * A2 * l2 + l6 * (7 * A3 + 9 * A4 * l2)) / cos(l),
    asin(sin(l) / M)
  ];
};
function equalEarth_default() {
  return projection(equalEarthRaw).scale(177.158);
}

// node_modules/d3-geo/src/projection/gnomonic.js
function gnomonicRaw(x, y2) {
  var cy = cos(y2), k2 = cos(x) * cy;
  return [cy * sin(x) / k2, sin(y2) / k2];
}
gnomonicRaw.invert = azimuthalInvert(atan);
function gnomonic_default() {
  return projection(gnomonicRaw).scale(144.049).clipAngle(60);
}

// node_modules/d3-geo/src/projection/naturalEarth1.js
function naturalEarth1Raw(lambda, phi) {
  var phi2 = phi * phi, phi4 = phi2 * phi2;
  return [
    lambda * (0.8707 - 0.131979 * phi2 + phi4 * (-0.013791 + phi4 * (3971e-6 * phi2 - 1529e-6 * phi4))),
    phi * (1.007226 + phi2 * (0.015085 + phi4 * (-0.044475 + 0.028874 * phi2 - 5916e-6 * phi4)))
  ];
}
naturalEarth1Raw.invert = function(x, y2) {
  var phi = y2, i2 = 25, delta;
  do {
    var phi2 = phi * phi, phi4 = phi2 * phi2;
    phi -= delta = (phi * (1.007226 + phi2 * (0.015085 + phi4 * (-0.044475 + 0.028874 * phi2 - 5916e-6 * phi4))) - y2) / (1.007226 + phi2 * (0.015085 * 3 + phi4 * (-0.044475 * 7 + 0.028874 * 9 * phi2 - 5916e-6 * 11 * phi4)));
  } while (abs(delta) > epsilon && --i2 > 0);
  return [
    x / (0.8707 + (phi2 = phi * phi) * (-0.131979 + phi2 * (-0.013791 + phi2 * phi2 * phi2 * (3971e-6 - 1529e-6 * phi2)))),
    phi
  ];
};
function naturalEarth1_default() {
  return projection(naturalEarth1Raw).scale(175.295);
}

// node_modules/d3-geo/src/projection/orthographic.js
function orthographicRaw(x, y2) {
  return [cos(y2) * sin(x), sin(y2)];
}
orthographicRaw.invert = azimuthalInvert(asin);
function orthographic_default() {
  return projection(orthographicRaw).scale(249.5).clipAngle(90 + epsilon);
}

// node_modules/d3-geo/src/projection/stereographic.js
function stereographicRaw(x, y2) {
  var cy = cos(y2), k2 = 1 + cos(x) * cy;
  return [cy * sin(x) / k2, sin(y2) / k2];
}
stereographicRaw.invert = azimuthalInvert(function(z2) {
  return 2 * atan(z2);
});
function stereographic_default() {
  return projection(stereographicRaw).scale(250).clipAngle(142);
}

// node_modules/d3-geo/src/projection/transverseMercator.js
function transverseMercatorRaw(lambda, phi) {
  return [log(tan((halfPi + phi) / 2)), -lambda];
}
transverseMercatorRaw.invert = function(x, y2) {
  return [-y2, 2 * atan(exp(x)) - halfPi];
};
function transverseMercator_default() {
  var m2 = mercatorProjection(transverseMercatorRaw), center = m2.center, rotate = m2.rotate;
  m2.center = function(_2) {
    return arguments.length ? center([-_2[1], _2[0]]) : (_2 = center(), [_2[1], -_2[0]]);
  };
  m2.rotate = function(_2) {
    return arguments.length ? rotate([_2[0], _2[1], _2.length > 2 ? _2[2] + 90 : 90]) : (_2 = rotate(), [_2[0], _2[1], _2[2] - 90]);
  };
  return rotate([0, 0, 90]).scale(159.155);
}

// node_modules/@nivo/geo/dist/nivo-geo.es.js
var import_jsx_runtime = __toESM(require_jsx_runtime());
function I() {
  return I = Object.assign ? Object.assign.bind() : function(e3) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var o2 = arguments[r2];
      for (var t2 in o2)
        Object.prototype.hasOwnProperty.call(o2, t2) && (e3[t2] = o2[t2]);
    }
    return e3;
  }, I.apply(this, arguments);
}
var D = { azimuthalEqualArea: azimuthalEqualArea_default, azimuthalEquidistant: azimuthalEquidistant_default, gnomonic: gnomonic_default, orthographic: orthographic_default, stereographic: stereographic_default, equalEarth: equalEarth_default, equirectangular: equirectangular_default, mercator: mercator_default, transverseMercator: transverseMercator_default, naturalEarth1: naturalEarth1_default };
var z = function(r2) {
  var o2 = r2.width, t2 = r2.height, i2 = r2.projectionType, n2 = r2.projectionScale, u = r2.projectionTranslation, l = r2.projectionRotation, c = r2.fillColor, d = r2.borderWidth, s = r2.borderColor, f = (0, import_react.useMemo)(function() {
    return D[i2]().scale(n2).translate([o2 * u[0], t2 * u[1]]).rotate(l);
  }, [o2, t2, i2, n2, u[0], u[1], l[0], l[1], l[2]]), h = (0, import_react.useMemo)(function() {
    return path_default(f);
  }, [f]), p = (0, import_react.useMemo)(function() {
    return graticule();
  }), g = Et(), v = (0, import_react.useMemo)(function() {
    return "function" == typeof d ? d : function() {
      return d;
    };
  }, [d]), m2 = We(s, g), b = (0, import_react.useMemo)(function() {
    return "function" == typeof c ? c : function() {
      return c;
    };
  }, [c]);
  return { projection: f, path: h, graticule: p, getBorderWidth: v, getBorderColor: m2, getFillColor: b };
};
var K = function(r2) {
  var o2 = r2.features, t2 = r2.data, i2 = r2.match, n2 = r2.label, a = r2.value, l = r2.valueFormat, c = r2.colors, d = r2.unknownColor, s = r2.domain, f = (0, import_react.useMemo)(function() {
    return (0, import_isFunction.default)(i2) ? i2 : function(e3, r3) {
      var o3 = (0, import_get.default)(e3, i2), t3 = (0, import_get.default)(r3, i2);
      return o3 && o3 === t3;
    };
  }, [i2]), h = (0, import_react.useMemo)(function() {
    return (0, import_isFunction.default)(n2) ? n2 : function(e3) {
      return (0, import_get.default)(e3, n2);
    };
  }, [n2]), p = (0, import_react.useMemo)(function() {
    return (0, import_isFunction.default)(a) ? a : function(e3) {
      return (0, import_get.default)(e3, a);
    };
  }, [a]), g = (0, import_react.useMemo)(function() {
    return void 0 === l ? function(e3) {
      return e3;
    } : (0, import_isFunction.default)(l) ? l : format(l);
  }, [l]), v = (0, import_react.useMemo)(function() {
    return tt(c).domain(s);
  }, [c, s]), m2 = (0, import_react.useMemo)(function() {
    return function(e3) {
      return void 0 === e3.value ? d : v(e3.value);
    };
  }, [v, d]), C = (0, import_react.useMemo)(function() {
    return o2.map(function(e3) {
      var r3 = t2.find(function(r4) {
        return f(e3, r4);
      }), o3 = p(r3);
      if (r3) {
        var i3 = I({}, e3, { data: r3, value: o3, formattedValue: g(o3) });
        return i3.color = m2(i3), i3.label = h(i3), i3;
      }
      return e3;
    });
  }, [o2, t2, f, p, g, m2]), b = j({ scale: v, valueFormat: g });
  return { colorScale: v, getFillColor: m2, boundFeatures: C, valueFormatter: g, legendData: b };
};
var V = (0, import_react.memo)(function(e3) {
  var r2 = e3.feature;
  return void 0 === r2.data ? null : (0, import_jsx_runtime.jsx)(w, { id: r2.label, color: r2.color, enableChip: true, value: r2.formattedValue });
});
V.propTypes = { feature: import_prop_types.default.object.isRequired }, V.displayName = "ChoroplethTooltip";
var A = { features: import_prop_types.default.arrayOf(import_prop_types.default.shape({ id: import_prop_types.default.string.isRequired, type: import_prop_types.default.oneOf(["Feature"]).isRequired, properties: import_prop_types.default.object, geometry: import_prop_types.default.object.isRequired })).isRequired, projectionType: import_prop_types.default.oneOf(Object.keys(D)).isRequired, projectionScale: import_prop_types.default.number.isRequired, projectionTranslation: import_prop_types.default.arrayOf(import_prop_types.default.number).isRequired, projectionRotation: import_prop_types.default.arrayOf(import_prop_types.default.number).isRequired, fillColor: import_prop_types.default.oneOfType([import_prop_types.default.string, import_prop_types.default.func]).isRequired, borderWidth: import_prop_types.default.oneOfType([import_prop_types.default.number, import_prop_types.default.func]).isRequired, borderColor: Ye.isRequired, enableGraticule: import_prop_types.default.bool.isRequired, graticuleLineWidth: import_prop_types.default.number.isRequired, graticuleLineColor: import_prop_types.default.string.isRequired, isInteractive: import_prop_types.default.bool.isRequired, onMouseEnter: import_prop_types.default.func.isRequired, onMouseMove: import_prop_types.default.func.isRequired, onMouseLeave: import_prop_types.default.func.isRequired, onClick: import_prop_types.default.func.isRequired, tooltip: import_prop_types.default.any, layers: import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([import_prop_types.default.oneOf(["graticule", "features"]), import_prop_types.default.func])).isRequired };
var J = I({}, A, { role: import_prop_types.default.string.isRequired });
var Q = I({ pixelRatio: import_prop_types.default.number.isRequired }, A);
var U = { data: import_prop_types.default.arrayOf(import_prop_types.default.object).isRequired, match: import_prop_types.default.oneOfType([import_prop_types.default.string, import_prop_types.default.func]).isRequired, label: import_prop_types.default.oneOfType([import_prop_types.default.string, import_prop_types.default.func]).isRequired, value: import_prop_types.default.oneOfType([import_prop_types.default.string, import_prop_types.default.func]).isRequired, valueFormat: import_prop_types.default.oneOfType([import_prop_types.default.string, import_prop_types.default.func]), colors: dt.isRequired, domain: import_prop_types.default.arrayOf(import_prop_types.default.number).isRequired, unknownColor: import_prop_types.default.string.isRequired, layers: import_prop_types.default.arrayOf(import_prop_types.default.oneOfType([import_prop_types.default.oneOf(["graticule", "features", "legends"]), import_prop_types.default.func])).isRequired };
var X3 = I({}, J, U, { role: import_prop_types.default.string.isRequired });
var Y = I({}, Q, U);
var Z = { projectionType: "mercator", projectionScale: 100, projectionTranslation: [0.5, 0.5], projectionRotation: [0, 0, 0], enableGraticule: false, graticuleLineWidth: 0.5, graticuleLineColor: "#999999", fillColor: "#dddddd", borderWidth: 0, borderColor: "#000000", isInteractive: true, onMouseEnter: function() {
}, onMouseLeave: function() {
}, onMouseMove: function() {
}, onClick: function() {
}, layers: ["graticule", "features"], legends: [], fill: [], defs: [] };
var $ = I({}, Z, { role: "img" });
var _ = I({}, Z, { pixelRatio: "undefined" != typeof window && window.devicePixelRatio || 1 });
var ee = { match: "id", label: "id", value: "value", colors: "PuBuGn", unknownColor: "#999", tooltip: V, layers: ["graticule", "features", "legends"] };
var re = I({}, $, ee, { role: "img" });
var oe = I({}, _, ee);
var te = (0, import_react.memo)(function(e3) {
  var r2 = e3.path, o2 = e3.graticule, t2 = e3.lineWidth, i2 = e3.lineColor;
  return (0, import_jsx_runtime.jsx)("path", { fill: "none", strokeWidth: t2, stroke: i2, d: r2(o2()) });
});
te.propTypes = { path: import_prop_types.default.func.isRequired, graticule: import_prop_types.default.func.isRequired, lineWidth: import_prop_types.default.number.isRequired, lineColor: import_prop_types.default.string.isRequired }, te.displayName = "GeoGraticule";
var ie = (0, import_react.memo)(function(e3) {
  var r2, o2 = e3.feature, t2 = e3.path, i2 = e3.fillColor, n2 = e3.borderWidth, a = e3.borderColor, u = e3.onClick, l = e3.onMouseEnter, c = e3.onMouseMove, d = e3.onMouseLeave;
  return (0, import_jsx_runtime.jsx)("path", { fill: null != (r2 = null == o2 ? void 0 : o2.fill) ? r2 : i2, strokeWidth: n2, stroke: a, strokeLinejoin: "bevel", d: t2(o2), onMouseEnter: function(e4) {
    return l(o2, e4);
  }, onMouseMove: function(e4) {
    return c(o2, e4);
  }, onMouseLeave: function(e4) {
    return d(o2, e4);
  }, onClick: function(e4) {
    return u(o2, e4);
  } }, o2.id);
});
ie.propTypes = { feature: import_prop_types.default.shape({ id: import_prop_types.default.string.isRequired, type: import_prop_types.default.oneOf(["Feature"]).isRequired, properties: import_prop_types.default.object, geometry: import_prop_types.default.object.isRequired }).isRequired, path: import_prop_types.default.func.isRequired, fillColor: import_prop_types.default.string.isRequired, borderWidth: import_prop_types.default.number.isRequired, borderColor: import_prop_types.default.string.isRequired, onMouseEnter: import_prop_types.default.func.isRequired, onMouseMove: import_prop_types.default.func.isRequired, onMouseLeave: import_prop_types.default.func.isRequired, onClick: import_prop_types.default.func.isRequired }, ie.displayName = "GeoMapFeature";
var ne = (0, import_react.memo)(function(e3) {
  var r2 = e3.width, i2 = e3.height, n2 = e3.margin, u = e3.features, l = e3.layers, s = e3.projectionType, f = e3.projectionScale, h = e3.projectionTranslation, p = e3.projectionRotation, g = e3.fillColor, m2 = e3.borderWidth, C = e3.borderColor, b = e3.enableGraticule, y2 = e3.graticuleLineWidth, R2 = e3.graticuleLineColor, j2 = e3.isInteractive, q = e3.onClick, T = e3.tooltip, M2 = e3.role, W = Bt(r2, i2, n2), w2 = W.margin, k2 = W.outerWidth, F = W.outerHeight, O = z({ width: r2, height: i2, projectionType: s, projectionScale: f, projectionTranslation: h, projectionRotation: p, fillColor: g, borderWidth: m2, borderColor: C }), L = O.graticule, E = O.path, S = O.getFillColor, G = O.getBorderWidth, x = O.getBorderColor, B = Et(), P = k(), N = P.showTooltipFromEvent, I2 = P.hideTooltip, D2 = (0, import_react.useCallback)(function(e4, r3) {
    return j2 && q && q(e4, r3);
  }, [j2, q]), K2 = (0, import_react.useCallback)(function(e4, r3) {
    return j2 && T && N((0, import_jsx_runtime.jsx)(T, { feature: e4 }), r3);
  }, [j2, N, T]), V2 = (0, import_react.useCallback)(function(e4, r3) {
    return j2 && T && N((0, import_jsx_runtime.jsx)(T, { feature: e4 }), r3);
  }, [j2, N, T]), A5 = (0, import_react.useCallback)(function() {
    return j2 && I2();
  }, [j2, I2]);
  return (0, import_jsx_runtime.jsx)(_i, { width: k2, height: F, margin: w2, theme: B, role: M2, children: l.map(function(r3, o2) {
    return "graticule" === r3 ? true !== b ? null : (0, import_jsx_runtime.jsx)(te, { path: E, graticule: L, lineWidth: y2, lineColor: R2 }, "graticule") : "features" === r3 ? (0, import_jsx_runtime.jsx)(import_react.Fragment, { children: u.map(function(e4) {
      return (0, import_jsx_runtime.jsx)(ie, { feature: e4, path: E, fillColor: S(e4), borderWidth: G(e4), borderColor: x(e4), onMouseEnter: K2, onMouseMove: V2, onMouseLeave: A5, onClick: D2 }, e4.id);
    }) }, "features") : (0, import_jsx_runtime.jsx)(import_react.Fragment, { children: r3(e3) }, o2);
  }) });
});
ne.displayName = "GeoMap", ne.propTypes = J, ne.defaultProps = $;
var ae = Mi(ne);
var ue = function(e3) {
  return (0, import_jsx_runtime.jsx)(Jt, { children: function(r2) {
    var o2 = r2.width, t2 = r2.height;
    return (0, import_jsx_runtime.jsx)(ae, I({ width: o2, height: t2 }, e3));
  } });
};
var le = function(e3, r2, o2, t2) {
  var i2 = Fi(r2, e3), n2 = i2[0], a = i2[1];
  return o2.find(function(e4) {
    return contains_default(e4, t2.invert([n2, a]));
  });
};
var ce = (0, import_react.memo)(function(e3) {
  var r2 = e3.width, t2 = e3.height, u = e3.margin, l = e3.pixelRatio, d = e3.features, s = e3.layers, f = e3.projectionType, h = e3.projectionScale, p = e3.projectionTranslation, g = e3.projectionRotation, m2 = e3.fillColor, C = e3.borderWidth, b = e3.borderColor, y2 = e3.enableGraticule, R2 = e3.graticuleLineWidth, j2 = e3.graticuleLineColor, q = e3.isInteractive, T = e3.onClick, M2 = e3.onMouseMove, W = e3.tooltip, w2 = (0, import_react.useRef)(null), k2 = Et(), F = Bt(r2, t2, u), O = F.margin, L = F.outerWidth, E = F.outerHeight, S = z({ width: r2, height: t2, projectionType: f, projectionScale: h, projectionTranslation: p, projectionRotation: g, fillColor: m2, borderWidth: C, borderColor: b }), G = S.projection, x = S.graticule, B = S.path, P = S.getFillColor, N = S.getBorderWidth, I2 = S.getBorderColor;
  (0, import_react.useEffect)(function() {
    if (w2) {
      w2.current.width = L * l, w2.current.height = E * l;
      var r3 = w2.current.getContext("2d");
      r3.scale(l, l), r3.fillStyle = k2.background, r3.fillRect(0, 0, L, E), r3.translate(O.left, O.top), B.context(r3), s.forEach(function(o2) {
        "graticule" === o2 ? true === y2 && (r3.lineWidth = R2, r3.strokeStyle = j2, r3.beginPath(), B(x()), r3.stroke()) : "features" === o2 ? d.forEach(function(e4) {
          r3.beginPath(), B(e4), r3.fillStyle = P(e4), r3.fill();
          var o3 = N(e4);
          o3 > 0 && (r3.strokeStyle = I2(e4), r3.lineWidth = o3, r3.stroke());
        }) : o2(r3, e3);
      });
    }
  }, [w2, L, E, O, l, k2, B, x, P, N, I2, d, s]);
  var D2 = k(), K2 = D2.showTooltipFromEvent, V2 = D2.hideTooltip, A5 = (0, import_react.useCallback)(function(e4) {
    if (q && W) {
      var r3 = le(e4, w2.current, d, G);
      r3 ? K2((0, import_jsx_runtime.jsx)(W, { feature: r3 }), e4) : V2(), M2 && M2(r3 || null, e4);
    }
  }, [K2, V2, q, W, w2, d, G]), J2 = (0, import_react.useCallback)(function() {
    return q && V2();
  }, [q, V2]), Q2 = (0, import_react.useCallback)(function(e4) {
    if (q && T) {
      var r3 = le(e4, w2.current, d, G);
      r3 && T(r3, e4);
    }
  }, [q, w2, d, G, T]);
  return (0, import_jsx_runtime.jsx)("canvas", { ref: w2, width: L * l, height: E * l, style: { width: L, height: E, cursor: q ? "auto" : "normal" }, onMouseMove: A5, onMouseLeave: J2, onClick: Q2 });
});
ce.displatName = "GeoMapCanvas", ce.propTypes = Q, ce.defaultProps = _;
var de = Mi(ce);
var se = function(e3) {
  return (0, import_jsx_runtime.jsx)(Jt, { children: function(r2) {
    var o2 = r2.width, t2 = r2.height;
    return (0, import_jsx_runtime.jsx)(de, I({ width: o2, height: t2 }, e3));
  } });
};
var fe = (0, import_react.memo)(function(e3) {
  var r2 = e3.width, i2 = e3.height, n2 = e3.margin, u = e3.features, l = e3.data, s = e3.match, f = e3.label, h = e3.value, g = e3.valueFormat, m2 = e3.projectionType, C = e3.projectionScale, b = e3.projectionTranslation, y2 = e3.projectionRotation, R2 = e3.colors, j2 = e3.domain, q = e3.unknownColor, T = e3.borderWidth, M2 = e3.borderColor, W = e3.enableGraticule, w2 = e3.graticuleLineWidth, k2 = e3.graticuleLineColor, F = e3.layers, O = e3.legends, L = e3.isInteractive, E = e3.onClick, S = e3.tooltip, G = e3.role, x = e3.defs, B = void 0 === x ? re.defs : x, N = e3.fill, D2 = void 0 === N ? re.fill : N, V2 = Bt(r2, i2, n2), A5 = V2.margin, J2 = V2.outerWidth, Q2 = V2.outerHeight, U2 = z({ width: r2, height: i2, projectionType: m2, projectionScale: C, projectionTranslation: b, projectionRotation: y2, fillColor: function() {
  }, borderWidth: T, borderColor: M2 }), X4 = U2.graticule, Y3 = U2.path, Z3 = U2.getBorderWidth, $2 = U2.getBorderColor, _2 = K({ features: u, data: l, match: s, label: f, value: h, valueFormat: g, colors: R2, unknownColor: q, domain: j2 }), ee2 = _2.getFillColor, oe2 = _2.boundFeatures, ne2 = _2.legendData, ae2 = Et(), ue2 = Hi(B, oe2, D2, { dataKey: "data", targetKey: "fill" }), le2 = k(), ce2 = le2.showTooltipFromEvent, de2 = le2.hideTooltip, se2 = (0, import_react.useCallback)(function(e4, r3) {
    return L && E && E(e4, r3);
  }, [L, E]), fe2 = (0, import_react.useCallback)(function(e4, r3) {
    return L && S && ce2((0, import_jsx_runtime.jsx)(S, { feature: e4 }), r3);
  }, [L, ce2, S]), he2 = (0, import_react.useCallback)(function(e4, r3) {
    return L && S && ce2((0, import_jsx_runtime.jsx)(S, { feature: e4 }), r3);
  }, [L, ce2, S]), pe2 = (0, import_react.useCallback)(function() {
    return L && de2();
  }, [L, de2]);
  return (0, import_jsx_runtime.jsx)(_i, { width: J2, height: Q2, margin: A5, theme: ae2, defs: ue2, role: G, children: F.map(function(e4, o2) {
    return "graticule" === e4 ? true !== W ? null : (0, import_jsx_runtime.jsx)(te, { path: Y3, graticule: X4, lineWidth: w2, lineColor: k2 }, "graticule") : "features" === e4 ? (0, import_jsx_runtime.jsx)(import_react.Fragment, { children: oe2.map(function(e6) {
      return (0, import_jsx_runtime.jsx)(ie, { feature: e6, path: Y3, fillColor: ee2(e6), borderWidth: Z3(e6), borderColor: $2(e6), onMouseEnter: fe2, onMouseMove: he2, onMouseLeave: pe2, onClick: se2 }, e6.id);
    }) }, "features") : "legends" === e4 ? O.map(function(e6, o3) {
      return (0, import_jsx_runtime.jsx)(X, I({ containerWidth: r2, containerHeight: i2, data: ne2 }, e6), o3);
    }) : (0, import_jsx_runtime.jsx)(import_react.Fragment, { children: e4({}) }, o2);
  }) });
});
fe.displayName = "Choropleth", fe.propTypes = X3, fe.defaultProps = re;
var he = Mi(fe);
var pe = function(e3) {
  return (0, import_jsx_runtime.jsx)(Jt, { children: function(r2) {
    var o2 = r2.width, t2 = r2.height;
    return (0, import_jsx_runtime.jsx)(he, I({ width: o2, height: t2 }, e3));
  } });
};
var ge = function(e3, r2, o2, t2) {
  var i2 = Fi(r2, e3), n2 = i2[0], a = i2[1];
  return o2.find(function(e4) {
    return contains_default(e4, t2.invert([n2, a]));
  });
};
var ve = (0, import_react.memo)(function(e3) {
  var r2 = e3.width, t2 = e3.height, u = e3.margin, l = e3.pixelRatio, d = e3.features, s = e3.data, f = e3.match, h = e3.label, p = e3.value, g = e3.valueFormat, m2 = e3.projectionType, C = e3.projectionScale, b = e3.projectionTranslation, y2 = e3.projectionRotation, R2 = e3.colors, j2 = e3.domain, q = e3.unknownColor, T = e3.borderWidth, M2 = e3.borderColor, W = e3.enableGraticule, w2 = e3.graticuleLineWidth, k2 = e3.graticuleLineColor, F = e3.layers, O = e3.legends, L = e3.isInteractive, E = e3.onClick, S = e3.onMouseMove, G = e3.tooltip, x = (0, import_react.useRef)(null), B = Et(), P = Bt(r2, t2, u), D2 = P.margin, V2 = P.outerWidth, A5 = P.outerHeight, J2 = z({ width: r2, height: t2, projectionType: m2, projectionScale: C, projectionTranslation: b, projectionRotation: y2, fillColor: function() {
  }, borderWidth: T, borderColor: M2 }), Q2 = J2.projection, U2 = J2.graticule, X4 = J2.path, Y3 = J2.getBorderWidth, Z3 = J2.getBorderColor, $2 = K({ features: d, data: s, match: f, label: h, value: p, valueFormat: g, colors: R2, unknownColor: q, domain: j2 }), _2 = $2.getFillColor, ee2 = $2.boundFeatures, re2 = $2.legendData;
  (0, import_react.useEffect)(function() {
    if (x) {
      x.current.width = V2 * l, x.current.height = A5 * l;
      var e4 = x.current.getContext("2d");
      e4.scale(l, l), e4.fillStyle = B.background, e4.fillRect(0, 0, V2, A5), e4.translate(D2.left, D2.top), X4.context(e4), F.forEach(function(o2) {
        "graticule" === o2 ? true === W && (e4.lineWidth = w2, e4.strokeStyle = k2, e4.beginPath(), X4(U2()), e4.stroke()) : "features" === o2 ? ee2.forEach(function(r3) {
          e4.beginPath(), X4(r3), e4.fillStyle = _2(r3), e4.fill();
          var o3 = Y3(r3);
          o3 > 0 && (e4.strokeStyle = Z3(r3), e4.lineWidth = o3, e4.stroke());
        }) : "legends" === o2 && O.forEach(function(o3) {
          H(e4, I({}, o3, { data: re2, containerWidth: r2, containerHeight: t2, theme: B }));
        });
      });
    }
  }, [x, V2, A5, D2, l, B, X4, U2, _2, Y3, Z3, ee2, O, F]);
  var oe2 = k(), te2 = oe2.showTooltipFromEvent, ie2 = oe2.hideTooltip, ne2 = (0, import_react.useCallback)(function(e4) {
    if (L && G) {
      var r3 = ge(e4, x.current, ee2, Q2);
      r3 ? te2((0, import_jsx_runtime.jsx)(G, { feature: r3 }), e4) : ie2(), S && S(r3 || null, e4);
    }
  }, [te2, ie2, L, G, x, ee2, Q2]), ae2 = (0, import_react.useCallback)(function() {
    return L && ie2();
  }, [L, ie2]), ue2 = (0, import_react.useCallback)(function(e4) {
    if (L && E) {
      var r3 = ge(e4, x.current, ee2, Q2);
      r3 && E(r3, e4);
    }
  }, [L, x, ee2, Q2, E]);
  return (0, import_jsx_runtime.jsx)("canvas", { ref: x, width: V2 * l, height: A5 * l, style: { width: V2, height: A5, cursor: L ? "auto" : "normal" }, onMouseMove: ne2, onMouseLeave: ae2, onClick: ue2 });
});
ve.displayName = "ChoroplethCanvas", ve.propTypes = Y, ve.defaultProps = oe;
var me = Mi(ve);
var Ce = function(e3) {
  return (0, import_jsx_runtime.jsx)(Jt, { children: function(r2) {
    var o2 = r2.width, t2 = r2.height;
    return (0, import_jsx_runtime.jsx)(me, I({ width: o2, height: t2 }, e3));
  } });
};
export {
  he as Choropleth,
  me as ChoroplethCanvas,
  oe as ChoroplethCanvasDefaultProps,
  Y as ChoroplethCanvasPropTypes,
  re as ChoroplethDefaultProps,
  X3 as ChoroplethPropTypes,
  ae as GeoMap,
  de as GeoMapCanvas,
  _ as GeoMapCanvasDefaultProps,
  Q as GeoMapCanvasPropTypes,
  $ as GeoMapDefaultProps,
  J as GeoMapPropTypes,
  pe as ResponsiveChoropleth,
  Ce as ResponsiveChoroplethCanvas,
  ue as ResponsiveGeoMap,
  se as ResponsiveGeoMapCanvas,
  D as projectionById,
  K as useChoropleth,
  z as useGeoMap
};
//# sourceMappingURL=@nivo_geo.js.map
